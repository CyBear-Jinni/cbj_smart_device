import 'package:cbj_smart_device/core/permissions/permissions.dart';

///  These class manage the permissions of user, both user permissions and
///  user ability to give permissions
class PermissionsManager {
  PermissionsManager(this.permissionsToGivePermissions, this.permissions);

  ///  Save the permissions of the user
  Permission permissions;

  ///  Save the permissions of the user to give permissions to others
  Permission permissionsToGivePermissions;

//  Set

//  Get
}
